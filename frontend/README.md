# JingWei Frontend

React 18 + TypeScript + Vite frontend for the JingWei production management system.

## Scripts

```bash
pnpm install
pnpm dev
pnpm lint
pnpm test
pnpm build
```

The Vite dev server proxies `/api` requests to `http://localhost:8080`.
